# SB Stocks Web Client

React + Vite frontend for the SB Stocks paper-trading platform.

Run from this directory:

```bash
npm install
npm run dev
```

For a non-local API, set `VITE_API_URL` in `.env`.
